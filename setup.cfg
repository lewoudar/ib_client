[tool:pytest]
testpaths = tests
addopts = --cov=infoblox --cov-report html

[flake8]
max-line-length = 120
exclude =
    .git,
    __pycache__,
    htmlcov,
    tests,
    .pytest_cache,
    .coverage,
    *.json